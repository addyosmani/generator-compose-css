language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-compose-css' ]; then cd .. && eval "mv $currentfolder generator-compose-css" && cd generator-compose-css; fi

